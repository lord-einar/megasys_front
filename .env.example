# API Configuration
# For development: keep as localhost:4000
# For production: can leave empty and use VITE_BACKEND_HOST instead
# If set, this takes precedence over VITE_BACKEND_HOST
VITE_API_URL=http://localhost:4000/api

# Backend Host - used in production
# This is more flexible than VITE_API_URL because it works with any domain
# Examples:
# - Azure: megasys-api.azurewebsites.net
# - Custom domain: megasys.megatlon.com.ar (requires backend at this domain)
# If not set, defaults to: megasys-api.azurewebsites.net
VITE_BACKEND_HOST=megasys-api.azurewebsites.net

# Backend Base URL for file downloads (optional)
# If not set, defaults will be used based on VITE_BACKEND_HOST
# VITE_BACKEND_URL=http://localhost:4000

# Application
VITE_APP_NAME=megasys
VITE_APP_VERSION=1.0.0
