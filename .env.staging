# Staging Environment Variables for megasys
# Used when building for staging: npm run build with --mode staging
# This configuration will be embedded in the compiled JavaScript

# Backend Host - staging API endpoint
# Note: Uses the -staging slot in Azure App Service
VITE_BACKEND_HOST=megasys-api-staging.azurewebsites.net

# Application
VITE_APP_NAME=megasys
VITE_APP_VERSION=1.0.0-staging

# Environment
NODE_ENV=staging
